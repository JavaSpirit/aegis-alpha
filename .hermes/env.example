# Hermes provider credentials.
# Copy needed keys into ~/.hermes/.env.
# Never commit real API keys.

# Primary provider: DeepSeek direct.
DEEPSEEK_API_KEY=

# Optional contrast/fallback provider. Do not route DeepSeek through OpenRouter.
OPENROUTER_API_KEY=

