# Hermes provider credentials.
# Copy needed keys into ~/.hermes/.env.
# Never commit real API keys.

# Fallback provider: DeepSeek direct.
DEEPSEEK_API_KEY=

# Primary provider: OpenRouter. Do not route DeepSeek through OpenRouter.
OPENROUTER_API_KEY=
